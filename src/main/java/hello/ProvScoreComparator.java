//package hello;
//
//import static org.mockito.Matchers.anyInt;
//
//import java.util.Comparator;
//
//import hello.domain.provScore;
//
//class ProvScoreComparator implements Comparator<provScore> {
//    @Override
//    public int compare(provScore a, provScore b) {
//        return a.getScore() < b.getScore() ? 1 : a.getScore() == b.getScore() ? 0 : -1;
//    }
//}
